{
  "name": "verzel",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
  },
  "scripts": {
    "dev": "sudo docker-compose up -d --build && cd back-end && npm run db",
    "db": "sudo docker-compose -f docker-compose.app.yml up --build && cd back-end && npm run db",
    "back": "cd back-end && yarn &&  yarn dev",
    "front": "cd front-end && yarn && tmux yarn dev",
    "studio": "cd back-end && npx prisma studio"
  },
  "author": "Marcos",
  "devDependencies": {
  }
}
